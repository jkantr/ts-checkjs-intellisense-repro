const {} = require('@node-sc2/core/constants/unit-type');

// make sure to `npm install` first.. then:
// ^ ctrl+space with cursor between the {} above
// now comment out / delete the tsconfig.json and try it again